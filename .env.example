# Server
PORT=3000

# MongoDB Atlas credentials
DB_USER=yourMongoAtlasUser
DB_PASSWORD=yourMongoAtlasPassword

# Firebase Admin service account (base64 of JSON from Google Cloud)
# Generate using: node keyConvert.js > fb_key.b64
# Then paste the single-line content into FB_SERVICE_KEY
FB_SERVICE_KEY=
